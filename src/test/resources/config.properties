# ServeRest API Configuration
base.uri=https://serverest.dev
base.path=/

# Timeouts (in milliseconds)
connection.timeout=10000
socket.timeout=10000

# Logging
enable.request.logging=true
enable.response.logging=true
